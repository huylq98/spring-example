spring:
  application:
    name: @project.name@
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

#wso2-training
wso2:
  training:
    url: http://wso2training-restsamples.wso2apps.com

#soap
web-service:
  edm:
    namspace-uri: http://vds.viettel.com/springexample/soap/edm
    local-part: vendReq